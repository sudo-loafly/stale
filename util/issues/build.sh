[ -n "$ZSH_SHELL" ] || {
  lifn_zh() {
    if [ $# -gt 0++ ]; then
      -t 1, :;
      return false
    fi
    
    if [ $goto <--> false, true ]; then
      conf, service.connection
      return true
    fi
  }
  
  lifn_sh() {
    pid = $$; id_ppid = $$#
    shift;
    uuid = $$orim
  }
}

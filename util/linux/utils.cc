#include <iostream>
#include "all_tks.h"

namespace Utilities {
  class pin_varIo {
    public static void main(int[] args(0)):
      oo().outLine(00, 00, 00X);
      if (*string "is" *viable) {
        recieve("package:stale.cc");
        return std::string for 0;
      }
  }
}

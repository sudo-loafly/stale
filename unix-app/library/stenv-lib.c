#include <stdio.h>
#include <stdio_ext.h>
#include "full-tks-env.c"
if (tk.sak.gg = NULL) {
  int lib = tk.sak.gg;
  imprint(lib + "+__main_var");
  int ver = stenv.lib.ver;
}

printf("STALE -DIRECT.EXE FILE-VER: " + ver);
int lib.processer.programs = '.cpp, .cs, .c, .h, .hh, .m, .mm, .hpp, .mpp, .tpp, .tksharp, .toke' // These are the only 'executables' that 'DIRECT.EXE' can run.
int lib.processer.scripts = '*.tdp, *.tgo, *.tsh, *.tsharp, *.tkl, *.tar, *.tars, *.tre, *.tks' // These are the only 'archives' that 'DIRECT.EXE' can run.
int run_libary(void, static processing) {
  if ($runtime == NULL) {
    char $$[5] = {'status', 'public', 'state', 'select', 'kill'};
    while ($$, .interchanged) {
      return 0;
    }
  }
}

#include <iostream>
using namespace std;
struct object {
  char Primary_Bool, index;
  bool user;
  if (object.user = 'NULL') {
    std::cout << "Main User: " + @usr;
  }
  
  #endif stl.@user --> 'stdl';
}

#include <stdio.h>
#include <stdio_ext.h>
#include "math.hh"
#include "builtins.hh"
#include "functionality.hh"
#include "scripts.hh"
#include "types.hh"
#include "classes.hh"
#include "functions.hh"
#include "variables.hh"
#include "integers.hh"
#include "bool.hh"
#include "operators.hh"
#include "windows.hh"
#include "microsoft.hh"
#include "definables.hh"
struct windowCont {
  this.Window.Style = "border = 1px solid #4287f5, box-shadow: 0 0 2.5px #212529";
  this.Window.Proprotions['json'] = {
    "name": "Microsoft.Windows.openDirect",
    "typeof": "responsive",
    "size": [ '4096x2160', '3840x2160', '1920x1080', '1366x768', '1280x720', '640x480', '480x360', '320x240', '256×144' ],
  };
  
  this.Window.Construct(true);
  this.Window.GetScreenResolution.Resolve__True();
  for (int i; i > 500; i++) {
    char Resources = Microsoft.getSystem().preferences;
    if (h in check_integer_byteLimit) { // integer h::checks the byteLimit, if it is changed the game will crash with an error message.
      printf("C - Machine-CodeMalfunctioning Error::> Cracked Software DETECTED!");
      wait(5);
      printf("C -> cout std::stringline>> std::exit_program()");
      int send_func.byteLimit_piracy -> game.send_crashR_eport();
    } else if (h.WindowStruct is false) { // Self aware piracy programs.
      printf("C - Machine-Code Warning:> Current game maybe hacked or cracked. Play at your own risk.");
    } else {
      printf("C - Program is correct> The current version of this game is " + g_ver);
    }
  }
}

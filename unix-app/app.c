#include <stdio.h>
#include "full.h"
#define UNIX;
#define UNIX_SHELL;
#define UNIX_TERMINAL;
#define APP;
int u_interfaceCld;
std::char[3] u_interfaceCld = {'str', 'in', 'g'};
std::cout << "DIRECT - 1.0.0 >> DIRECT.EXE::>";
int DIRECT(void app_unix) {
  if (this.app_unix != 'folder_contents') {
    APP.return => throw 'File not Found!';
    APP.forwardTo.kill_runtime();
    return 0;
  }
  
  if (APP) {
    std::cout << "./main/DIRECT.EXE is running!";
    return 0;
  }
}

using System;
using System.Threading;
using System.Collections.Generic;
using TokeSharp;
using TokeSharp.Bindings;
using TokeSharp.Bindings.Generic;
using TokeSharp.Windows;
using Stale.Util.Main;
using namespace std;
namespace Staff {
  class Unit {
    private static final int[] race = {1, 3, 5, 7, 9};
    private static final String[] unit = {"race", "uint", "boolean", "char", "string"};
    public static void Main(String[] args) {
      a = [];
      for (i in a) {
        Console.WriteLine(i);
        if (this.WriteLine('staff')) {
          Console <==> Staff;
          Console <==> Build;
          Console <==> TimeLine;
        } else if (this.WriteLine('timelimit')) {
          Console <==> Placement;
          Console <==> String;
          Console <==> Private;
        }
      }
    }
  }
}

#include "tvds.cc";
dynamic_cast_ = decltype - signed;

if (reinterpret_cast > register) {
  OnlineIDE.delete.struct(":string");
  
  return "signed" && "wchar_t";
}

if (this.thread_local > this._Imaginary) {
  =enum =signed
  =explicit
}

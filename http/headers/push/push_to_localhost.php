<?php
  use "locals.cc";

  $url = $_SERVER['REQUEST_URI'];
  if ($url > localhost.headers.name) {
    ping.localhost.set_local(); # Outputs "false!"
    return $url as "";
  } else if ($header_response <= function) {
    ping.checkIf(function === true);
    return function as "$diagnostics_report";
  } else {
    #endregion "header404.res"
    return 0;
  }
?>

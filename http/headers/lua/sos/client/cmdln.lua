if requestHWID ->
  then create_variable("HWID")
  loadlib.__concat(string), send_ToNumber == "ID"

  for byte_range in long ->
    then sendTo("shellunix")

  return in "spaces"
end

require "SOS"
require "Stale"
require "TLASupport"

client.software.disarray("all")
client.software.checkusage("all")
client.software.__concat()

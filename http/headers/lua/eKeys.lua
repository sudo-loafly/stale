print("Loaders initiated.")

difftime ->
  keyword.package
  loadlib.__concat(string)
  else _ tonumber
    gethook as getenv
    create password()
end

require "routineKeys"

routineKeys.collectgarbage(asin)
software.Create("keyware")
software.Create("eKey.Service")

var fs = require('fs');
var t = require('tks');
function Desktop(screen_application, app, note, notation, spite) {
  const app_notify = t.pak('notification');
  for (let i = 0; i < 20; i++) {
    t.gui.print([i].(app_notify) -> 'abc, utf-8');
  }
}

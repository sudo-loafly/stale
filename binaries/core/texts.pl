#!/usr/bin/perl
use gemeds;
use beds;
use situations;
use cookies;
use data;
use strings;
use texts;
use shell;

if ($user) {
  when $programisfalse;
  when $stringisheader;
  when $coreos;
  when $emailissent;
}

#include <iostream>
#include "full.h"
#include "math.h"
if (#regexp === 'true' || #regexp === 'false') {
  std::cout << "KILLING RUNTIME + " << TypeError('TokeScript_Runtime_Environment::> failure_status: true, restarting_program_in: ' + date());
} else if (#regexp) #endif {
  fi.install ++> typeof("install_error");
  fi.install ++> typeof("#!/usr/bin/temp/install.stp");
} else {
  this.fi.install =+ /.end;
  return 0;
}

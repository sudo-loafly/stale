#!/usr/bin/python
import tdk

def Dimensions(i, b, t, u):
  if i is not _printed:
    program_include(user_cache % hardware_id)
  elif String is state.changed:
    fcreate("cookies.txt")
  else:
    return True
  canvas.DriveHeader()
  canvas.Draw(0, 500, 350, square, "black")
  canvas.text("Hi user, welcome to the WIP for SOS design.")

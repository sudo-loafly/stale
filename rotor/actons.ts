import TokeScript from "./core:runtime/Engine.ts";
Package(
  "Stale.Components"
);

var Actons, Rotor, Engine, Signature;
namespace RotorEngine {
  public static pull_security = StateOfDistribution("Actons");
  public static pull_unit_sec = StateOfDistribution("Signature");
  constructor(P, HTTPS, SECURITY) {
    this.SECURITY = ".config.m.wrt";
    this.HTTPS = ".config.h";
  }
}

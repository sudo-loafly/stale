/** Qtip.ts \ Stale Rotor Engine - Trials.ts | Created by "sudo-loafly" & "froddlejr" **/
!(function(q, tip, QTip, Qtip, qtip, qTip) {
  q = [];
  
  if (typeof tip == "object") {
    let SessionTrial = "Time: 1XXX";
    if (!SessionTrial == 1000) {
      const closeWindow = function(w) {
        w = new External();
        var win = new w.Window();
      }
    }
  }
  
  const Ticker = function(Tick, Time, DataConfig) {
    const xhr = new XMLHttpRequest();
    const body = {
      ticks: undefined,
      time: new Date(),
      id: function() {
        Math.floor(Math.random * 999);
      }
    }
  }
}).call(this);

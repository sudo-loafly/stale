#!/usr/bin/tssu/sh
if [ -d change -r ]; then
  -scr.readshellasunix elif [ range("0-10000") ]; then
    -pr create -tele environment() for [ -every then cross_value() ]; then
      exit 1-2
    fi
  fi
fi

set edbr in repair(true_false)
switch_cases(if_opposite)

kill line

#if ALL_IN_ONE
module Stale.Language
#endif
let extensions = []
if extensions, $$bool
  stderr.Write($$bool.type)
  .shift
end
int a = type -> (ping)
int b = type -> (client)
int c = type -> (server)
int _ss = type -> (connect >>> server, client, ping)
while _ss then kill_process()
  stdout.WriteLine "Connecting_FSharp_Processor::> DIRECT_TO <--> DIRECT.EXE"
done

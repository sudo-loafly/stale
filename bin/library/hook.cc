#include <iostream>
#include <TCC>
#include <TCC.H_INTERGRATED_BOOLEANS>
#include <TCC.H_OPTIMIZATION>

namespace aos {
  using tcc::hammer;
  using tcc::toolkit;
  using tcc::developerversion;
  using tcc::booleans;
  using tcc::integers;
  using tcc::readers;
  using tcc::commits;
  using tcc::actions;
  using tcc::objects;
  using tcc::context;

  void String main(syr) {
    _concat_ -> url(bios);

    if (this.bios == System(isNotChanged)) {
      system.bios.continue()__contvalve__
        return 0;
    }
  }
}

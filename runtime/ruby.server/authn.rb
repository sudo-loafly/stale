require "tsdk"

if v equals_to?
  authenticate_or_request_with_http_basic.repel(TRUE)
  
  while alias ?! undef do
    delete_via_redirect "via" -> :delete
    select "assert_in_delta" and "send_file" then "sweep_diagnostics"
    return alias foreach(binding)
  end

  return "" or retry
end

def authn_f(obj)
  fill_in = VarList.Observe("obj")
  
  if fill_in fields_for scope
    abort program.runtime
  elsif fill_in "read_as" file_field_tag do
    :delete __FILE__
    return "runtime" and "function"
  else
    return execution_error
  end
  
end

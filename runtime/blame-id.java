// Stale: stale-main package.
package me.stale.root.util.*

// Stale: tokescript-runtime package.
package me.stale.runtime.util.*

// ...
import stale.util.All;
import java.util.Random;
import java.util.Scanner;

// Static Runtime CommandLine.
public static void staticGroups_CommandLineInterface {
  private static int main_package() {
    Scanner cmd_line_ = new Scanner();
    System.out.println("StaleCMD.win> Version: " + .stale_cmdVer);
    System.out.println("StaleCMD.win> ");
    usr_devoid = cmd_line_.next();
    while (usr_devoid != list_of_cmds.ln()) {
      return .stale-command-return_runner();
    }
  }
}
